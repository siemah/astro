---
import ProductCard from "../../../components/product-card/index.astro";
import Layout from "../../../layouts/Layout.astro";
import getCategories from "../../../helpers/services/products/categories";
import { getProductsOfCategory } from "../../../helpers/services/products/all";
import getCategoryBySlug from "../../../helpers/services/products/categories/by-slug";
import ProductsInfiniteList from "../../../components/products-infinite-list.astro";

const { slug } = Astro.params;
const category = await getCategoryBySlug(slug);

export async function getStaticPaths({ paginate }) {
  const categories = await getCategories();
  console.log("[<<<<<<<<gSP>>>>>>>>]", categories.length);
  const paths: any[] = [];
  for (const category of categories) {
    const slug = decodeURIComponent(category.slug);
    const productPerPage = 24;
    const productsByCategory = await getProductsOfCategory(category.id);
    console.log(
      `<<<<<<<<<<<<[${category.name} ${category.id}]>>>>>>>>>>>>`,
      productsByCategory?.length,
    );
    // const productsByCategory = products.filter((product) => {
    //   return product?.category_ids?.includes(category?.term_id);
    // });

    paths.push(
      paginate(productsByCategory, {
        params: { slug },
        pageSize: productsByCategory.length,
      }),
    );
  }

  return paths.flat();
}
const { page } = Astro.props;

const products = page?.data??[];
console.log(`length of products`, products.length);
---

<Layout
  title={category?.name}
  image={category?.image || ""}
  description={category?.description}
>
  <main class="main-container js-main__container">
    <!-- hero section -->
    <section class="relative bg-primary text-white">
      <div 
        class="absolute inset-0 opacity-40 bg-cover bg-center" 
        style={`background-image: url('${category?.image}');`}
        ></div>
      <div class="container mx-auto px-4 py-20 relative z-10 text-center">
          <h1 class="text-4xl md:text-5xl font-bold mb-4">{category?.name}</h1>
          <p class="text-xl max-w-3xl mx-auto">{category?.description}</p>
      </div>
    </section>
    <!-- end of hero section -->

    <section class="section overflow-x-hidden">
      <div class="container mx-auto">
      <!-- breadcrumb menu -->
        <nav aria-label="Breadcrumb" class="w-full">
          <ol class="flex items-center flex-wrap space-x-2 text-sm">
            <li class="flex items-center">
              <a
                href="/"
                class="flex items-center text-gray-500 hover:text-gray-900 dark:text-gray-400 dark:hover:text-gray-100"
              >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="16"
                height="16"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                class="h-4 w-4"
              >
                <path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z" />
                <polyline points="9 22 9 12 15 12 15 22" />
              </svg>
                <span class="sr-only">Accueil</span>
              </a>
            </li>
            <li class="flex items-center">
              <svg
              xmlns="http://www.w3.org/2000/svg"
              width="16"
              height="16"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              class="h-4 w-4 text-gray-400"
            >
              <path d="m9 18 6-6-6-6" />
            </svg>
            </li>
            <li class="flex items-center">
              <a
                href="/shop/1"
                class="text-gray-500 hover:text-gray-900 dark:text-gray-400 dark:hover:text-gray-100"
              >
                Boutique
              </a>
            </li>
            <li class="flex items-center">
              <svg
              xmlns="http://www.w3.org/2000/svg"
              width="16"
              height="16"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              class="h-4 w-4 text-gray-400"
            >
              <path d="m9 18 6-6-6-6" />
            </svg>
            </li>
            <li class="flex items-center">
              <svg
              xmlns="http://www.w3.org/2000/svg"
              width="16"
              height="16"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              class="h-4 w-4 text-gray-400"
            >
              <path d="m9 18 6-6-6-6" />
            </svg>
            </li>
            <li class="flex items-center">
              <span class="text-gray-900 font-medium dark:text-gray-100" aria-current="page">
                {category?.name}
              </span>
            </li>
          </ol>
        </nav>
        <!-- end of breadcrumb menu -->
      </div>
    </section>

  <ProductsInfiniteList products={products} />

  </main>
  <!-- end main content -->
</Layout>
